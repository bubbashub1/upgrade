(function(){
'use strict';
const C=window.BubbaHubConfig||{};const root=document.getElementById('bubbahub-app');if(!root)return;
const roles=(C.user&&C.user.roles)||[];if(!C.user||!roles.some(r=>['administrator','bubbahub_leader','leader','leaderpro'].includes(r)))return;
const esc=s=>String(s??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'}[c]));
async function api(path,opt={}){opt.headers=Object.assign({'Content-Type':'application/json'},opt.headers||{});if(C.nonce)opt.headers['X-WP-Nonce']=C.nonce;opt.credentials='same-origin';const r=await fetch(C.api+path,opt);let j={};try{j=await r.json()}catch(e){}if(!r.ok)throw new Error(j.message||'Request failed ('+r.status+')');return j;}
const fieldGroups=[
{id:'basics',label:'Basics',icon:'✦',intro:'Start with the essentials your families need to recognise your listing.',fields:[['category','Category','text'],['tags','Tags','text'],['is_featured','Featured','checkbox']]},
{id:'location',label:'Location',icon:'⌖',intro:'Tell families where they can find you.',fields:[['address','Address','text'],['city','Town / City','text'],['region','Region / County','text'],['zip','Postcode','text'],['manual_lat','Latitude','text'],['manual_lng','Longitude','text']]},
{id:'contact',label:'Contact',icon:'@',intro:'Add the ways families can contact or discover you.',fields:[['website','Website','url'],['email','Email','email'],['facebook','Facebook','url'],['instagram','Instagram','url']]},
{id:'sessions',label:'Sessions & pricing',icon:'◷',intro:'Give parents the practical information they will want before attending.',fields:[['price','Price','text'],['is_free','Free','checkbox'],['term_time','Term Time','text'],['age_range','Age Range','text'],['session_length','Session Length','text'],['day','Day','text'],['sen','SEN Friendly','text'],['timetable','Timetable','textarea'],['openinghours','Opening Hours','textarea']]},
{id:'media',label:'Media',icon:'▧',intro:'Add imagery or other media references for your listing.',fields:[['images','Images','text']]}
];
function inputField(key,label,type,value){value=value??'';if(type==='checkbox')return `<label class="bh-dir-check"><input type="checkbox" data-field="${esc(key)}" ${value==='1'||value===1||value===true?'checked':''}><span><strong>${esc(label)}</strong><small>Show this option on your listing</small></span></label>`;if(type==='textarea')return `<label class="bh-dir-field bh-dir-field-full"><span>${esc(label)}</span><textarea data-field="${esc(key)}" rows="5" placeholder="Enter ${esc(label.toLowerCase())}…">${esc(value)}</textarea></label>`;const attrs=type==='url'?'type="url"':type==='email'?'type="email"':'type="text"';return `<label class="bh-dir-field"><span>${esc(label)}</span><input ${attrs} data-field="${esc(key)}" value="${esc(value)}" placeholder="Enter ${esc(label.toLowerCase())}…"></label>`;}
function buildSections(post,isNew){const custom=post&&post.custom_fields?post.custom_fields:{};const sections=fieldGroups.map(group=>({...group,html:group.fields.map(([k,l,t])=>inputField(k,l,t,custom[k]?.value??'')).join('')}));const customFields=[];Object.keys(custom).forEach(k=>{if(fieldGroups.some(g=>g.fields.some(f=>f[0]===k)))return;const f=custom[k]||{};customFields.push(inputField(k,f.label||k,f.type||'text',f.value??''));});if(customFields.length)sections.push({id:'custom',label:'Additional information',icon:'＋',intro:'Extra fields configured for your BubbaHub listing.',html:customFields.join('')});return sections;}
function editorShell(title,subtitle,post,type,isNew){const sections=buildSections(post,isNew),content=post?.raw_content||'',status=post?.status||'draft';const nav=sections.map((s,i)=>`<button type="button" class="bh-dir-nav ${i===0?'is-active':''}" data-step="${esc(s.id)}"><span class="bh-dir-nav-icon">${s.icon}</span><span>${esc(s.label)}</span></button>`).join('');const panes=sections.map((s,i)=>`<section class="bh-dir-pane ${i===0?'is-active':''}" data-pane="${esc(s.id)}"><div class="bh-dir-pane-head"><div><span class="bh-dir-kicker">${i+1} of ${sections.length}</span><h3>${esc(s.label)}</h3><p>${esc(s.intro)}</p></div></div>${s.id==='basics'?`<label class="bh-dir-field bh-dir-field-full"><span>Listing name <em>*</em></span><input id="bh-leader-title" required value="${esc(post?.title||'')}" placeholder="e.g. Baby Sensory Torbay"></label><label class="bh-dir-field bh-dir-field-full"><span>Description</span><textarea id="bh-leader-content" rows="8" placeholder="Tell families what makes your group or service special…">${esc(content)}</textarea></label>`:''}<div class="bh-dir-grid">${s.html}</div></section>`).join('');return `<div class="bh-dir-editor"><header class="bh-dir-editor-head"><div class="bh-dir-breadcrumb"><button type="button" id="bh-leader-cancel">Leader Portal</button><span>/</span><strong>${isNew?'New listing':'Edit listing'}</strong></div><div class="bh-dir-title-row"><div><span class="bh-dir-kicker">${isNew?'CREATE LISTING':'EDIT LISTING'}</span><h1>${esc(title)}</h1><p>${esc(subtitle)}</p></div><div class="bh-dir-head-actions"><span class="bh-dir-status ${status==='publish'?'is-live':''}">${status==='publish'?'Published':status==='pending'?'Pending review':'Draft'}</span></div></div></header><form id="bh-leader-form" class="bh-dir-layout"><aside class="bh-dir-sidebar"><div class="bh-dir-sidebar-title">Listing details</div><nav>${nav}</nav><div class="bh-dir-help"><strong>Need help?</strong><p>Complete the sections that apply to your group. You can save as a draft at any time.</p></div></aside><main class="bh-dir-content"><div class="bh-dir-card">${panes}</div><section class="bh-dir-publish"><div><span class="bh-dir-kicker">PUBLISHING</span><h3>Ready to publish?</h3><p>New listings are saved as drafts. Your administrator can review and publish them.</p></div><label class="bh-dir-status-field"><span>Status</span><select id="bh-leader-status"><option value="draft" ${status==='draft'?'selected':''}>Draft</option><option value="pending" ${status==='pending'?'selected':''}>Pending review</option><option value="publish" ${status==='publish'?'selected':''}>Published</option></select></label></section><section class="bh-dir-sync"><span class="bh-dir-sync-icon">↕</span><div><strong>Google Sheets sync</strong><p id="bh-google-message">${post?.google_writeback_enabled?'Changes will be written back to the linked Google Sheet when you save.':'Google write-back is not currently enabled for this listing.'}</p></div></section><div class="bh-dir-actions"><button type="button" class="bh-dir-btn secondary" id="bh-leader-cancel-bottom">Cancel</button><div class="bh-dir-save-group"><span id="bh-leader-result" role="status"></span><button class="bh-dir-btn primary" type="submit" id="bh-leader-save">${isNew?'Create listing':'Save changes'} <span>→</span></button></div></div></main></form></div>`;}
function collect(){const meta={};root.querySelectorAll('#bh-leader-form [data-field]').forEach(el=>{meta[el.dataset.field]=el.type==='checkbox'?(el.checked?'1':'0'):el.value;});return {title:root.querySelector('#bh-leader-title').value.trim(),content:root.querySelector('#bh-leader-content').value,status:root.querySelector('#bh-leader-status').value,meta};}
function setupWizard(){const buttons=[...root.querySelectorAll('.bh-dir-nav')],panes=[...root.querySelectorAll('.bh-dir-pane')];buttons.forEach(btn=>btn.onclick=()=>{const step=btn.dataset.step;buttons.forEach(b=>b.classList.toggle('is-active',b===btn));panes.forEach(p=>p.classList.toggle('is-active',p.dataset.pane===step));const content=root.querySelector('.bh-dir-content');if(content)content.scrollIntoView({behavior:'smooth',block:'start'});});}
async function openEditor(type,id){try{const post=id?await api(`leader/${type}/${id}`):null;root.innerHTML=editorShell(post?post.title:'New listing',type==='groups'?'Create or update your local group listing.':'Create or update your event.',post,type,!id);setupWizard();const cancel=()=>renderPortal();root.querySelector('#bh-leader-cancel').onclick=cancel;root.querySelector('#bh-leader-cancel-bottom').onclick=cancel;root.querySelector('#bh-leader-form').onsubmit=async e=>{e.preventDefault();const btn=root.querySelector('#bh-leader-save'),msg=root.querySelector('#bh-leader-result');btn.disabled=true;msg.textContent='Saving…';try{const data=collect();if(!data.title)throw new Error('Please enter a listing name.');const out=await api(`leader/${type}${id?'/'+id:''}`,{method:id?'PUT':'POST',body:JSON.stringify(data)});msg.textContent=out.message||'Listing saved.';btn.innerHTML='Saved ✓';setTimeout(()=>renderPortal(),900);}catch(err){msg.textContent=err.message;btn.disabled=false;}};}catch(e){root.innerHTML=`<div class="bh-main"><div class="bh-empty"><h2>Could not open listing</h2><p>${esc(e.message)}</p><button class="bh-btn" id="bh-retry">Back to Leader Portal</button></div></div>`;root.querySelector('#bh-retry').onclick=renderPortal;}}
function listingCard(item,type){const isEvent=type==='events',status=String(item.status||'draft').toLowerCase(),image=item.image||item.thumbnail||item.featured_image||item.image_url||'',location=item.city||item.town||item.region||'',linked=!!item.google_id,label=isEvent?'Event':'Group',action=isEvent?'Edit event':'Edit listing';return `<article class="bh-leader-card"><div class="bh-leader-card-media ${image?'has-image':'no-image'}" ${image?`style="background-image:url('${esc(image)}')"`:''}>${!image?`<span class="bh-leader-card-placeholder">${isEvent?'◷':'✦'}</span>`:''}<span class="bh-leader-card-type">${label}</span><span class="bh-leader-card-status ${status==='publish'?'is-live':status==='pending'?'is-pending':''}"><i></i>${status==='publish'?'Published':status==='pending'?'Pending':'Draft'}</span></div><div class="bh-leader-card-body"><div class="bh-leader-card-title-row"><h3>${esc(item.title||'Untitled')}</h3><span class="bh-leader-card-more">•••</span></div><div class="bh-leader-card-meta">${location?`<span>⌖ ${esc(location)}</span>`:''}${linked?`<span class="is-linked">↕ Google linked</span>`:`<span>Google not linked</span>`}</div></div><div class="bh-leader-card-footer"><span class="bh-leader-card-id">${esc(action)}</span><button class="bh-leader-card-edit" data-edit-${isEvent?'event':'group'}="${item.id}">${action}<span>→</span></button></div></article>`;}
async function renderPortal(){root.innerHTML='<div class="bh-main"><div class="bh-empty"><p>Loading your listings…</p></div></div>';try{const groups=await api('leader/groups');let events=[];try{events=await api('leader/events')}catch(e){}const published=groups.filter(x=>String(x.status||'').toLowerCase()==='publish').length,drafts=groups.filter(x=>String(x.status||'').toLowerCase()!=='publish').length;root.innerHTML=`<div class="bh-main bh-leader bh-leader-portal"><div class="bh-pagehead bh-leader-pagehead"><div><div class="bh-eyebrow">Leader workspace</div><h1>Leader Portal</h1><p class="bh-small">Manage your BubbaHub listings from one place.</p></div><button class="bh-btn" id="bh-add-listing">＋ Add New Listing</button></div><div class="bh-leader-stats"><div class="bh-leader-stat"><span class="bh-leader-stat-icon">▣</span><div><strong>${groups.length}</strong><small>Total listings</small></div></div><div class="bh-leader-stat"><span class="bh-leader-stat-icon">✓</span><div><strong>${published}</strong><small>Published</small></div></div><div class="bh-leader-stat"><span class="bh-leader-stat-icon">◷</span><div><strong>${drafts}</strong><small>Needs attention</small></div></div></div><div class="bh-leader-toolbar"><div><h2>Your groups</h2><span>${groups.length} listing${groups.length===1?'':'s'}</span></div><button class="bh-btn secondary" id="bh-add-event">＋ Add Event</button></div><section class="bh-leader-card-grid">${groups.length?groups.map(g=>listingCard(g,'groups')).join(''):'<div class="bh-card"><h3>No groups yet</h3><p>Create your first listing using the button above.</p></div>'}</section><div class="bh-leader-toolbar bh-leader-events-heading"><div><h2>Your events</h2><span>${events.length} event${events.length===1?'':'s'}</span></div></div><section class="bh-leader-card-grid">${events.length?events.map(g=>listingCard(g,'events')).join(''):'<div class="bh-card"><p>No events yet.</p></div>'}</section></div>`;root.querySelector('#bh-add-listing').onclick=()=>openEditor('groups',0);root.querySelector('#bh-add-event').onclick=()=>openEditor('events',0);root.querySelectorAll('[data-edit-group]').forEach(b=>b.onclick=()=>openEditor('groups',+b.dataset.editGroup));root.querySelectorAll('[data-edit-event]').forEach(b=>b.onclick=()=>openEditor('events',+b.dataset.editEvent));}catch(e){root.innerHTML=`<div class="bh-main"><div class="bh-empty"><h2>Leader access is not working</h2><p>${esc(e.message)}</p><p class="bh-small">The WordPress REST request was rejected. Refresh and try again.</p><button class="bh-btn" id="bh-leader-refresh">Refresh</button></div></div>`;root.querySelector('#bh-leader-refresh').onclick=renderPortal;}}
function boot(){const page=C.initialPage||'';if(page==='leader'||location.pathname.replace(/\/+$/,'').endsWith('/leader'))renderPortal();}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else boot();
})();
